if (NOT DEFINED CACHE{libsDownloaded})
  set(ARCH_NAME "aarch64-linux-gnu")
  set(LOCAL_LIB_DIR "${CMAKE_CURRENT_SOURCE_DIR}/${ARCH_NAME}")
  file(MAKE_DIRECTORY ${LOCAL_LIB_DIR})
  execute_process(COMMAND ssh ostrostroj "sudo apt install libjack-jackd2-dev" COMMAND_ERROR_IS_FATAL ANY)
  execute_process(COMMAND scp ostrostroj:/usr/lib/${ARCH_NAME}/libjack.so ${LOCAL_LIB_DIR} COMMAND_ERROR_IS_FATAL ANY)
  execute_process(COMMAND scp ostrostroj:/usr/lib/${ARCH_NAME}/libdb-5.3.so ${LOCAL_LIB_DIR} COMMAND_ERROR_IS_FATAL ANY)
  set(libsDownloaded ON CACHE BOOL "")
endif()